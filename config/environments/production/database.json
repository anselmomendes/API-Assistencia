{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "bookshelf",
      "settings": {
        "client": "postgres",
        "host": "${process.env.DATABASE_HOST || 'ec2-52-23-14-156.compute-1.amazonaws.com'}",
        "port": "${process.env.DATABASE_PORT || 5432}",
        "database": "${process.env.DATABASE_NAME || 'd4n3oijs45dqs8'}",
        "username": "${process.env.DATABASE_USERNAME || 'drcaiqsmukpbtz'}",
        "password": "${process.env.DATABASE_PASSWORD || 'b9aacb855aa86f265e0ed085f3a99648cd8d121ef290d913f531d07119a8ead9'}"
      },
      "options": {}
    }
  }
}
